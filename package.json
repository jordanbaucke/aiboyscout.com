{
  "name": "aiboyscout.com",
  "version": "1.0.0",
  "description": "21st Century Digital Boy Scout website",
  "scripts": {
    "dev": "live-server --port=3000 --open=/index.html --watch=.",
    "start": "npm run dev"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
